spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/mercadolivre
    username: root
    password: ajudazupper
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        naming-strategy: org.hibernate.cfg.ImproveNamingStrategy